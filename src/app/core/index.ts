// ---- NGRX STORE ACTIONS and SELECTORS
export * from '@store/app.actions';
export * from '@store/app.selectors';

// ---- SINGLE ENTRY POINT: provideCore (init + providers)
export { provideCore } from './core';
